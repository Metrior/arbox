import React, {useState} from 'react';
import Button from "../common/Button";

const Poll = ({submitVote, poll}) => {
    return (
        <div>


            <Button/>
        </div>
    );
};

export default Poll;
